#include<stdio.h>
int main()
{

    int t,a,b;
    scanf("%d",&t);
    for(int i = 0; i < t; i++){

        scanf("%d%d",&a,&b);
        int sum = 0;
        for(int j = a; j <= b; j++){

            if(j%2)sum+=j;
        }
        printf("Case %d : %d",i+1,sum);
    }
    return 0;
}
